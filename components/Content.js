import styled from 'styled-components';

const Content = styled.div`
  padding: ${p => `${p.theme.space[4]}px ${p.theme.space[5]}px`};
  max-width: 1012px;
  background: #FFF;

  .Content__Inner {
  }
`

export default Content;
